#!/usr/bin/env node
import os from "node:os";
import path from "node:path";

import { runTraceShim } from "../lib/shim.mjs";

runTraceShim("CODEX_REAL_BIN", path.join(os.homedir(), ".local", "bin", "codex"));
